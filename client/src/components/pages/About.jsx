import { styled } from "@mui/material";

const MainDiv = styled("div")({
  height: "100vh",
  fontSize: "25px",
  textAlign: "center",
});

const About = () => {
  return (
    <div>
      <MainDiv>About page</MainDiv>
    </div>
  );
};

export default About;
