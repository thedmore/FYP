import { styled } from "@mui/material";

const MainDiv = styled("div")({
  height: "100vh",
  fontSize: "25px",
  textAlign: "center",
});

const FAQ = () => {
  return (
    <div>
      <MainDiv>FAQ page</MainDiv>
    </div>
  );
};

export default FAQ;
