import { styled } from "@mui/material";

const MainDiv = styled("div")({
  height: "100vh",
  fontSize: "25px",
  textAlign: "center",
});

const Contact = () => {
  return (
    <div>
      <MainDiv>Contact page</MainDiv>
    </div>
  );
};

export default Contact;
